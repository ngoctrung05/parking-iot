# FastAPI and Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
jinja2==3.1.2

# Database
sqlalchemy==2.0.25
alembic==1.13.1

# MQTT Client
paho-mqtt==1.6.1

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-dotenv==1.0.0

# Data Validation (Pydantic v2)
pydantic==2.5.3
pydantic-settings==2.1.0
email-validator==2.1.0

# Utilities
python-dateutil==2.8.2

# WebSocket support
websockets==12.0

# Rate Limiting
slowapi==0.1.9

# CORS middleware (included in fastapi but explicit for clarity)
starlette==0.35.1

# Optional: PostgreSQL driver (if using PostgreSQL instead of SQLite)
# psycopg2-binary==2.9.7

# Optional: MySQL driver (if using MySQL)
# pymysql==1.1.0

# Development Tools (optional - uncomment for development)
# pytest==7.4.3
# pytest-asyncio==0.21.1
# httpx==0.25.2  # For testing async APIs
